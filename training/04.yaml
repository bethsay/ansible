---
- name: Build machine setup
  hosts: all
  become: true
  tasks:
    - name: git installation
      apt:
        name: git             #check with "git -v"
        state: latest
    - name: maven installation
      apt:
        name: maven           #check with "mvn --version"
        state: latest
